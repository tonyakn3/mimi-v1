{
  "name": "mimi-live-interpreter",
  "version": "1.8.0",
  "private": true,
  "type": "module",
  "description": "Mimi V1.8 - Safari/iPhone PWA interpreter with state-locked wake word + intent command engine",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/js/app.js && node --check public/js/audio.js && node --check public/js/commands.js"
  },
  "engines": {
    "node": ">=20"
  }
}
