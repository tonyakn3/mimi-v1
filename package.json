{
  "name": "mimi-live-interpreter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mimi - Safari/iPhone PWA voice interpreter using Gemini Live API",
  "scripts": {
    "start": "node --env-file=.env server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/js/app.js && node --check public/js/audio.js && node --check public/js/commands.js"
  },
  "engines": {
    "node": ">=20"
  }
}
