{
  "name": "mimi-hot-live-interpreter",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Mimi V2.1 - hot Gemini Live session, per-turn local reset, Safari/iPhone voice interpreter.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/js/app.js && node --check public/js/audio.js && node --check public/js/commands.js"
  },
  "engines": { "node": ">=20" }
}
