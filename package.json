{
  "name": "mimi-live-interpreter",
  "version": "1.7.0",
  "private": true,
  "type": "module",
  "description": "Mimi V1.7 - Safari/iPhone PWA voice interpreter with robust two-way Vietnamese command detection",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/js/app.js && node --check public/js/audio.js && node --check public/js/commands.js"
  },
  "engines": {
    "node": ">=20"
  }
}
